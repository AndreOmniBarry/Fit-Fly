// Sidecar types for audio-cue.js (hand-written JS, untouched — see
// tsconfig.json).
export function primeAudio(): void;
export function playCompletionBeep(): void;
export function vibrateDevice(pattern?: number | number[]): void;
